| 序号      | 模块名称                                  | **训练期** 输入→输出 (B×C×H×W)                                                | 主要运算                                                                    | 对任务的作用                                                           | **推理期去留**         |
| ------- | ------------------------------------- | ---------------------------------------------------------------------- | ----------------------------------------------------------------------- | ---------------------------------------------------------------- | ----------------- |
| **0**   | **输入数据**                              | I<sub>raw</sub>(3×H×W)、D<sub>gt</sub>(1×H×W)、I<sub>gt</sub>(3×H×W)     | 同步裁剪/翻转 → Tensor；RGB 映射\[–1,1]；Depth 映射\[0,1]；生成半、全分辨率                  | 提供三路对齐张量                                                         | 仅 I<sub>raw</sub> |
| **1**   | **Shallow Feature Extractor (SFE)**   | raw → F₀<sup>raw</sup>(C×H×W)                                          | Conv3×3 → GN → GELU ×2                                                  | 初级纹理 & 色彩编码                                                      | 保留                |
| **2‑T** | **Depth Feature Extractor** *(训练期专用)* | D<sub>gt</sub>(1×H×W) → {F₀…F₃}<sup>dep</sup>                          | 每级: Conv3×3(s=2)→ReLU→Conv3×3→ReLU                                      | 生成多尺度真深度特征供 Cross‑Attention 与 Depth‑Gate 使用                      | **裁剪**            |
| **2‑S** | **Mono‑Depth Head** *(蒸馏学生)*          | F₀<sup>raw</sup> → **Ĝ<sub>depth</sub>**(1×H×W)                       | ↓2 Conv×2 → ↑2 ConvT×2 → Sigmoid                                        | 学习预测门控图；训练期用 `L_depth = ‖Ĝ–G_true‖` 蒸馏 F₀<sup>dep</sup>；推理期生成门控 | **保留**            |
| **3‑A** | **Raw Encoder** (L=4)                 | F₀<sup>raw</sup> … F₃<sup>raw</sup>                                    | 每级 Conv↓2 → (LN→MDTA→+→LN→GDFN→+)×2                                     | 多尺度 Transformer 特征                                               | 保留                |
| **3‑B** | **GT Encoder (Frozen)**               | 与 Raw Encoder 同尺寸                                                      | Conv↓2×2＋LN（权重冻结）                                                       | 提供清晰特征对齐基准                                                       | **裁剪**            |
| **3‑C** | **Teacher‑Student 对齐**                | F<sup>raw</sup>, F<sup>gt</sup>, Attn<sub>raw</sub>, Attn<sub>gt</sub> | L<sub>feat</sub> + KL(Attn)                                             | 强化结构与边缘，提升去模糊                                                    | 损失层自动忽略           |
| **3‑D** | **Cross‑Attention 融合**                | Q=F<sup>raw</sup><sub>i</sub>, K/V=F<sup>dep</sup><sub>i</sub>         | 在 Transformer 内执行 RGB←Depth 查询                                          | 利用精准深度优化注意力                                                      | **裁剪**            |
| **4**   | **Joint Bottleneck**                  | F₃<sup>fused</sup>(C×H/8×W/8)                                          | Restormer Block × N                                                     | 汇聚上下文                                                            | 保留                |
| **5‑A** | **Deblur Branch（高频）**                 | Bottleneck → Res<sub>D</sub>(3×H×W)                                    | 3 级上采：每级 PixelShuffle↑2 → **密集跳跃融合**+**自适应深度融合**→Transformer         | 密集跳跃积累细节特征；尺度感知MLP自适应融合深度特征；G\_src=F<sup>dep</sup>训练期 / Ĝ推理期   | 保留                |
| **5‑B** | **Color Branch（低频）**                  | Bottleneck → Res<sub>C</sub>(3×H×W)                                    | 上采至 H×W；中层 Depth‑Gate；GlobalAvgPool→MLP→(γ,β)；`Res_C=γ⊙I_raw+β`         | 区分前后景色偏、校正通道                                                     | 保留                |
| **6**   | **融合重建**                              | I\_raw, Res<sub>D</sub>, Res<sub>C</sub> → Ĩ                           | Ĩ = tanh(I\_raw + Res\_D + Res\_C)                                      | 输出最终增强图                                                          | 保留                |
| **6\*** | **Depth 监控输出**                        | F₀<sup>dep</sup> 或 Ĝ<sub>depth</sub>                                  | —                                                                       | 训练期用于蒸馏/可视化；推理可省                                                 | 可选                |
