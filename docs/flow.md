原始 RGB ──► SFE（浅层特征） ──► 编码器（第1次前向，纯RGB） ──► 主干瓶颈
                                │
                                │ skip_feats (RGB跳连特征)
                                ▼
        DepthDecoder（深度解码器）◄─────────┘  (输入瓶颈输出+RGB跳连)
           │
           ├─ depth_pred   (1×H×W，连续深度图)
           └─ depth_feats  (L=4: 48➜96➜192➜384通道，多尺度深度特征)

depth_feats ──► 1×1投影层 ──► 编码器（第2次前向，RGB⊕Depth联合编码）
                                        │
                                        └─ student_feats  (RGB特征)
                                               │
                                               ▼
                           主干瓶颈 (Restormer堆叠)
                                               │
               ┌───────────────────────────────┴─────────────────────────────┐
               │                                                             │
           去模糊分支 (res_d)                                         颜色分支 (res_c)
               │                                                             │
               └───────── 物理A/D模型融合（最后一步） ───────────────────────┘
                                               │
                                               ▼
                                         增强RGB输出
