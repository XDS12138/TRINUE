python scripts/train.py --config configs/train.yaml --resume

python scripts/launch_tensorboard.py --port 6007

git add .
git commit -m "first commit"
git remote add origin git@github.com:XDS12138/TRINUE.git
git remote remove origin
git remote add origin git@github.com:XDS12138/TRINUE.git
git push -u origin main
置信度引导损失
这个文件重新写，双向交叉注意力，请阅读我现在的项目文件进行书写，我需要这样绘制，根据代码可以得出，


git commit -m "your message"
git push
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
Epoch 1/100:  60%|████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████▏                                                                                                       | 400/666 [03:02<01:34,  2.83it/s, Loss=20078.8926][CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
Epoch 1/100:  60%|████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████▏                                                                                                       | 400/666 [03:02<01:34,  2.83it/s, Loss=20057.7578]18:33:12 [ERROR] 记录可视化数据时发生错误: too many values to unpack (expected 4)，步骤: 400
18:33:12 [ERROR] Traceback (most recent call last):
  File "/root/autodl-tmp/TRINUE/utils/visualization_utils.py", line 96, in log_training_visualization
    _log_rgb_comparison(vis_raw_first_item, outputs.enhanced[0:1] if outputs.enhanced is not None else None,
  File "/root/autodl-tmp/TRINUE/utils/visualization_utils.py", line 216, in _log_rgb_comparison
    metric_logger.log_image("train/input", vis_input_normalized, step=step)
  File "/root/autodl-tmp/TRINUE/utils/logger.py", line 414, in log_image
    B, C, H, W = image.shape
ValueError: too many values to unpack (expected 4)

Epoch 1/100:  60%|████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████████▌                                                                                                       | 401/666 [03:03<02:09,  2.04it/s, Loss=20057.7578][CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
[CrossAttention] 保存窗口注意力图: shape=torch.Size([6, 8, 64, 64])
Epoch 1/100:  60%|█████████████████████████████████████████████████████████████████████████████████████████████████████


以下从“现有策略存在的局限性”与“可行的改进方向”两个角度进行分析，并结合常见的深度–RGB 融合方法，给出若干可落地的建议。

---

## 一、现有梯度‐置信度策略的局限性

1. **梯度置信度仅反映局部纹理信息**
   传统基于梯度的置信度计算（如利用深度图在空间域的梯度幅值或 Laplacian 方差）本质上只关注深度图局部像素处的锐利程度或边缘信息。虽然当深度边缘与 RGB 纹理边缘对齐时，这种度量可在一定程度上反映深度预测的“可靠度”，但也存在以下问题：

   * 对于平坦区域（如远处海底或同色区域），梯度趋近于零，此时置信度会被误判为“深度不可靠”，却不一定意味着深度预测不可用；
   * 在高噪声情况下（例如低光或海水浊度较高时），深度预测本身可能因输入噪声产生错误，梯度却依然可能较大，从而高估了深度可靠度；
   * 仅以一阶梯度或二阶梯度衡量，无法捕捉到深度估计中的系统性误差（如尺度漂移、平面歪斜等），缺乏对不确定性的全局或统计性度量。

2. **深度–RGB 融合边界模糊**
   在当前模型结构里，您采用了 Cross‐Attention 的方式，将投影到与 RGB 通道数相匹配的深度特征 `depth_feats` 与 RGB 特征进行融合。但如果对融合时所用的权重（`pred_gate`／`depth_conf_map`）仅依赖梯度信息，那么当深度预测在某些区域失真（例如低纹理、强散射或遮挡区域），梯度置信度可能并不能有效区分“深度预测错误”与“深度平坦区域”，从而导致融合时对“错误深度”没能及时抑制，使得 RGB 分支依赖了不真实的深度线索，出现过度平滑或伪影。

3. **置信度值缺乏可学习性**
   梯度度量大多属于“手工设计”的启发式算法，难以随着训练自动调整。也就是说，如果在不同场景（如海水浊度、光照变化）下，深度预测的误差模式不同，梯度度量并不能适配各种误差分布。相比之下，“可学习的置信度”往往通过一个小型网络或自监督损失，将“深度‐RGB 对齐误差”直接映射到置信图上，能更好地体现深度预测的不确定区域。

---

## 二、改进深度对 RGB 引导的几个方向

### 1. 引入可学习的深度不确定性/置信度预测模块

#### 1.1 基于深度回归残差的自监督置信度

* **思路**：在训练阶段，引入一个辅助网络（ConfidenceNet）专门输入深度预测 `depth_pred` 及 RGB 特征，学习输出“一张与深度预测同尺寸的置信度图”

  * 网络架构可十分轻量，例如：两层卷积 + Sigmoid。
  * 监督信号则来自于“深度一致性损失”：令 `depth_pred` 与真实深度 `depth_gt` 求差的绝对值（或 L1/L2），并将其在空间维度进行归一化后作为“真值置信度标签”（越小误差对应越高置信度），这样网络就能学会在“深度预测误差大”的区域输出低置信。

* **优势**：

  1. 置信度标签来源于深度预测与真实深度的实际误差，而非简单的梯度幅值，更具针对性；
  2. 网络参数可随训练迭代自动调整，从而适应不同的图片场景（泛化性更好）；
  3. 与 DepthDecoder 一同联合训练时，还能通过“深度重投影一致性”或“采样点投影一致性”引入更多无监督约束，使置信度估计更加可靠。

* **实现示例**（伪代码）：

  ```python
  class ConfidenceNet(nn.Module):
      def __init__(self, in_channels):
          super().__init__()
          # in_channels 可为 DepthDecoder 输出的多尺度融合特征通道数，也可为 1 (depth_pred) + 3 (RGB)
          self.conv1 = nn.Conv2d(in_channels, 32, 3, padding=1)
          self.conv2 = nn.Conv2d(32, 1, 3, padding=1)
      def forward(self, depth_pred, rgb_feature):
          # 这里以 depth_pred 和 RGB 特征 concat 作为输入
          x = torch.cat([depth_pred, rgb_feature], dim=1)
          x = F.relu(self.conv1(x))
          conf_map = torch.sigmoid(self.conv2(x))  # 输出 [0,1] 置信度
          return conf_map
  ```

  * **训练时**：令

    $$
      L_{\text{conf}} = \text{MSE}\bigl(\text{ConfidenceNet}(depth\_pred, rgb),\, \exp(-|depth\_pred - depth\_gt|)\bigr)
    $$

    或

    $$
      L_{\text{conf}} = \text{L1}\Bigl(\text{ConfidenceNet}(depth\_pred, rgb),\, 1 - \frac{|depth\_pred - depth\_gt|}{\max_{\text{batch}}|depth\_pred - depth\_gt|}\Bigr).
    $$

    通过将误差“反向映射”为 \[0,1] 置信值，让网络学习到更接近深度真实度量的置信度。

* **推理时**：直接用 `ConfidenceNet(depth_pred, rgb)` 得到一张可学习的置信度图，代替原先的梯度置信度，效果通常更稳定。

#### 1.2 基于深度复原一致性的自监督置信度

* **思路**：如果可获取一些简单的几何先验（例如相邻帧或数据增强版本的 RGB），可以在训练时引入“深度复原一致性”损失：令对原图进行小仿射变换（如平移、旋转、缩放）后得到 `I’`，再让网络分别生成 `depth_pred` 和 `depth_pred’`，然后将 `depth_pred’` 反变换到原坐标系，与 `depth_pred` 做差，越一致的区域说明深度预测可靠。将一致性置信度作为标签训练一个置信度分支，使得网络 “自监督” 地学习到哪些区域深度稳定。

* **实现思路概览**：

  1. 对原图 `I` 随机做小仿射变换，得到 `I’`；
  2. 用 DepthDecoder 分别预测 `depth_pred` 与 `depth_pred’`;
  3. 将 `depth_pred’` 进行逆仿射操作，记为 `depth_pred’_rev`;
  4. 计算一致性误差：

     $$
       E(x,y) = \bigl|\,depth\_pred(x,y)\;-\;depth\_pred’\_rev(x,y)\bigr|.
     $$
  5. 将 $1 - \frac{E}{\max(E)}$ 作为“自监督置信度标签”，训练置信度网络。

* **优势**：

  * 无需真实深度标注，即可在训练阶段获得更具几何一致性的置信度；
  * 对场景扰动有更强鲁棒性，尤其是在水下弱纹理区域。

---

### 2. 优化深度–RGB 跨模态融合机制

#### 2.1 多尺度可学习门控（Gated Fusion）

* **思路**：目前模型在 Cross‐Attention 融合时，用的是 DepthDecoder 生成的深度特征先经过线性投影，再与 RGB 特征做 attention。可以在每一层加入一个“可学习的融合门控”：

  $$
    F_{\text{fused}}^{(l)} \;=\; \sigma\bigl(W_{g}^{(l)} * [F_{\text{rgb}}^{(l)},\,F_{\text{depth}}^{(l)}]\bigr)\;\odot\;F_{\text{rgb}}^{(l)}
    \;+\;\bigl(1 - \sigma(\cdot)\bigr)\;\odot\;F_{\text{depth}}^{(l)},
  $$

  其中 $\sigma$ 是 Sigmoid，$[\,\cdot\,]$ 表示通道拼接，$W_g^{(l)}$ 为第 $l$ 层可学习卷积权重。这样让网络自动学习在每个尺度下 “更信任 RGB” 还是 “更信任深度”，而不依赖预先设定的置信度。

* **优势**：

  1. 完全由网络自适应权衡深度与 RGB；
  2. 对深度预测不准的区域，网络可学到更大幅度“抑制深度分支”；
  3. 不必人为设计梯度置信度、深度置信度等，简洁且更灵活。

#### 2.2 改进 Cross‐Attention 模块——借鉴多头注意力（Multi‐Head Attention）

* **思路**：如果您当前的 Cross‐Attention 只是单头（single‐head）或简单的通道融合，可以考虑使用多头注意力（Multi‐Head Attention）来分别捕捉“深度对 RGB 的不同空间/通道子空间引导”，公式示例如下：

  $$
    \text{Attention}(Q,K,V) = \text{softmax}\Bigl(\frac{QK^\top}{\sqrt{d_k}}\Bigr)V,
  $$

  在这里，让

  $$
    Q = W_Q\,F_{\text{rgb}},\quad
    K = W_K\,F_{\text{depth}},\quad
    V = W_V\,F_{\text{depth}},
  $$

  并拆分成 $h$ 个头，每个头学到一套 $(W_Q^{(i)},W_K^{(i)},W_V^{(i)})$。最后再拼接并线性映射为融合特征。

* **优势**：

  1. 多头注意力能让网络在深度与 RGB 融合时，以多个子空间视角衡量“深度特征对 RGB 关注区域”的重要性；
  2. 提高了跨模态融合的灵活性与表达能力，尤其当深度预测存在一定空间偏差时，多头注意力能在某些头上忽略错误深度，于其他头利用正确深度，最后再融合出更鲁棒的结果。

---

### 3. 深度信息的增强策略

#### 3.1 引入辅助几何约束（如 Surface Normal 或 上下文一致性）

* **思路**：在真实深度不可用时，除了直接用 DepthDecoder 预测的深度，还可以从深度图推导出“表面法向”（Surface Normal）或“深度曲率”，作为额外的几何线索，将其与 RGB 特征一起输入到后续的融合模块，丰富网络对几何结构的把握。

  * 例如，表面法向可由深度梯度 $(\partial_x d,\partial_y d)$ 计算得到：

    $$
      \mathbf{n}(x,y) \;=\; \frac{1}{\sqrt{\partial_x d^2 + \partial_y d^2 + 1}} 
      \bigl(-\partial_x d,\,-\partial_y d,\;1 \bigr).
    $$
  * 将 $\mathbf{n}$ 拼回深度特征通道一起作为多极度特征，为 RGB 引导提供更多 “本地平面” 的信息。

* **优势**：

  1. 仅依赖深度预测本身即可计算，无需额外标注；
  2. 表面法向对纹理边界及遮挡区域更敏感，可帮助网络识别“几何突变”处，避免深度–RGB 融合时产生光照伪影或透视畸变；
  3. 可与置信度联合使用：当法向变化剧烈但置信度较低时，对应区域可适当降低深度对 RGB 的引导权重。

#### 3.2 引入自监督结构一致性（如光度一致性损失）

* **思路**：如果您有前后连续帧，或可对图像做小仿射变换，可以在训练阶段同时对“基于深度的重投影”与“光度重建误差”做联合优化：

  $$
    L_{\text{photo}} = \sum_{x,y} \bigl\|I_t(x,y) - I_{t-1}\bigl(\pi\bigl(\,d_t(x,y)\bigr)\bigr)\bigr\|_1,
  $$

  其中 $\pi(\cdot)$ 表示将深度投影到上一帧坐标。通过最小化“深度预测导致 RGB 重投影误差”，网络被迫学到更符合真实场景几何的深度分支。自然，深度分支更好，融合到 RGB 时也更合理。

* **优势**：

  1. 无需多摄像头，只要有连续帧序列即可；
  2. 能够让 DepthDecoder 学到几何连贯的深度结构，从而在推理时给 RGB 提供更可靠的信息；
  3. 间接提高了模型对深度–RGB 融合的整体表现，因为深度本身更准确。

---

## 三、综合建议与落地方案

1. **替换梯度置信度为可学习的置信度网络**

   * 在 DepthDecoder 之后串联一个“置信度子网”，输入可以是 $\{depth\_pred,\;RGB\;bottleneck\;features\}$，输出一张 \[0,1] 范围的置信度图。
   * 训练阶段用 $\exp\bigl(-|depth\_pred - depth\_gt|\bigr)$ 或归一化后的深度误差作为监督信号；推理阶段则直接输出可学习置信度。
   * 实现后，将置信度图在“Cross‐Attention”或“Gated Fusion”时乘进深度特征通道，替代当前的梯度置信度。

2. **改造融合模块，引入多头注意力或门控机制**

   * 将原先单一的 Cross‐Attention 扩展为多头注意力，让网络多视角衡量深度对 RGB 的影响；
   * 或者直接实现“可学习门控（Gated Fusion）”，由网络自行学习在各通道/各尺度层级下的深度–RGB 加权系数。
   * 这两种方式都可在 `RawEncoder` 或解码器模块中替换现有的融合写法。

3. **在深度分支输出中加入法向或曲率特征**

   * 在 DepthDecoder 生成 $\{depth\_pred,\,depth\_feats\}$ 之后，计算对应的法向图（Surface Normal），并将其与 `depth_feats` 拼接为更丰富的几何特征。
   * 后续在 Cross‐Attention 时，让 RGB 特征同时 attend 到“深度特征 + 法向特征”，增强对几何边缘的敏感性。

4. **（可选）引入自监督一致性损失**

   * 如果手头有连续帧或数据增强手段，可以在训练时加入“光度一致性损失”或“深度—重投影一致性损失”，让 DepthDecoder 的输出在几何上更连贯、逼真。
   * 减少深度误差，自然也能让深度引导 RGB 时更加有效。

---

## 四、总结

* **现状问题**：基于一阶/二阶梯度计算置信度，难以全面反映深度预测的不确定性，且对纹理匮乏或噪声场景效果欠佳。
* **改进思路**：通过“可学习置信度网络”直接将深度预测误差映射为置信度，或者让网络通过“多头注意力 / 门控融合”自适应权衡深度与 RGB。此外，可以加入表面法向等几何先验，或者依据光度一致性引入自监督约束，进一步提升深度–RGB 融合的效果。
* **落地路径**：建议先从“置信度网络”切入，将当前梯度置信度替换为可学习置信度；随后可逐步优化融合机制，最后再考虑自监督一致性损失与几何先验的引入。

这样，深度分支便能在推理阶段以更可靠的“置信度”指导 RGB 分支，避免梯度度量带来的误判，最终提升整体增强和复原效果。若有进一步需求或遇到具体实现问题，欢迎继续交流。
